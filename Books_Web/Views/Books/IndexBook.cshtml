@* @using Microsoft.AspNetCore.Components

@model IEnumerable<Books_Web.Models.Book>



<div class="container mt-4">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary">Books List</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-controller="Books" asp-action="CreateBook" class="btn btn-outline-primary"><i class="bi bi-plus-square-fill"></i> Create New Book</a>
        </div>
    </div>

    <table class="table table-striped table-bordered">
        <thead class="thead-dark">
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Title</th>
                <th scope="col">Pages</th>
                <th scope="col">Actions</th> <!-- For Update and Delete buttons -->
            </tr>
        </thead>
        <tbody>
            @if (Model != null)
            {
                foreach (var book in Model)
                {
                    <tr>
                        <td>@book.Id</td>
                        <td>@book.Title</td>
                        <td>@book.Pages</td>
                        <td>
                            <a asp-controller="Books" asp-action="UpdateBook" asp-route-id="@book.Id" class="btn btn-success"><i class="bi bi-pencil-square"></i> Edit</a>
                            <a asp-controller="Books" asp-action="DeleteBook" asp-route-id="@book.Id" class="btn btn-danger"><i class="bi bi-trash-fill"></i> Delete</a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4" class="text-center">No books available.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
 *@